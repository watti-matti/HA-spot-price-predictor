# Fingrid Open Data API key (optional - Tier 3 features)
# Register for free at https://data.fingrid.fi
# Without this key, the model trains on Tier 1+2 features only (28-34 features)
# With this key, Tier 3 grid features are added (up to 38 features)
FINGRID_API_KEY=
